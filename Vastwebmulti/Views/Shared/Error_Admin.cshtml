@model System.Web.Mvc.HandleErrorInfo
@using Vastwebmulti.Models;
@{
    ViewBag.Title = "Error";
    Layout = null;
}
@{
    VastwebmultiEntities db = new VastwebmultiEntities();
    //var image = db.tblWhiteLabelLoginBackImages.Where(p => p.Role == "WHITELABEL").ToList();
    //ViewBag.image = image.Count == 0 ? "" : image.SingleOrDefault().ImageUrl;
}
<link href="https://fonts.googleapis.com/css?family=Roboto:400,700&subset=latin,cyrillic-ext" rel="stylesheet" type="text/css">
<link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet" type="text/css">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
<link href="~/MainFrontDesign/css/globalallcss.css" rel="stylesheet" />
<script src="~/Scripts/jquery-1.10.2.min.js"></script>
@*<h1 class="text-danger">Error.</h1>
    <h2 class="text-danger">An error occurred while processing your request.</h2>*@

<div class="section secondary-section fullbodydycolorbg" style="background:url(@Url.Content("~/FooterImages/"+ViewBag.image)">
    <div class="container centered">
        <div class="row-fluid">
            <div class="span-12">
                <center>
                    <p style="font-weight:700; font-size:28px; padding: 32px; color:red;margin-top:77px;">
                        <i class="fa fa-warning" style="font-size:28px;color:red"></i> &nbsp;Error
                    </p>
                </center>
            </div>
        </div>
        <div class="row-fluid" style="margin-top:28px; margin-bottom:30px;">
            <div class="span-12">
                <center>
                    <p style="color:red; font-size:22px;">
                        <b>An error occurred while processing your request.</b>
                    </p>
                </center>
            </div>

            <div class="span2"></div>

        </div>
    </div>
</div>
@if (!HttpContext.Current.Request.IsLocal)
{
    <!-- To Disable Inspect Element -->
    <script src="~/Scripts/Disable_Inspect_Element/Disable_Inspect.js"></script>
}