PAYOUT AND UPI COLLECTION API 

1. serviceallow (Table Name) =>

  a. InstantPayAccountVerify 
  b. InstantPayPayOut
  c. RazorpayUPI

  if use any api then status is true.

2. For Instantpay API Account Verify 

    instantpayon (table name)
 
    if use this then status is true

3. Insert Instantpay auth
   
   Instantpay_auth (table name)

   if you want to use the instant pay api then insert the instant pay auth
   in this table there is a column accountnumber this column is used only if we used 
   payout services.

4. Use Razorpay or Instant Pay 

   payout_switch (Table name)

   use this add column values
    
    apiname      status   
   VASTBAZAAR      Y
   INSTANTPAY      N
   RAZORPAY        N

5. Range wise API Switch 
    MoneyApiUrls (Action Name)

rang_of_imps_transaction_ALL_retailer(table name) 

6. Razorpay Auth

For use Razorpay auth razorpay_auth(table name)

7.  Retiler Wise Api Move

   MoneyApiUrls (Action)
   rang_of_imps_transaction_retailer(Table)